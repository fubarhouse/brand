<?php

/**
 * @file
 * Add menu items and pages for the module.
 */

/**
 * Implements hook_menu().
 *
 * @inheritdoc
 */
function brand_menu() {
    $items['admin/config/user-interface/brand'] = array(
    );
    return $items;
}
